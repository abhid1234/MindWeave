# Database
DATABASE_URL=postgresql://mindweave:dev_password_change_in_prod@localhost:5432/mindweave_dev

# Auth.js (NextAuth v5)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your_auth_secret_here
AUTH_URL=http://localhost:3000

# Google OAuth (optional)
# Get from: https://console.cloud.google.com/
AUTH_GOOGLE_ID=your_google_client_id
AUTH_GOOGLE_SECRET=your_google_client_secret

# Google Gemini API (for tagging, Q&A, summarization, embeddings, and all AI features)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_gemini_api_key_here

# Google Cloud Storage (optional — omit for local dev, files stored on disk)
# GCS_BUCKET_NAME=mindweave-uploads

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
