# Production Environment Variables for Google Cloud Run
# These are managed via Google Secret Manager

# Database (Cloud SQL)
# Format: postgresql://USER:PASSWORD@/DATABASE?host=/cloudsql/PROJECT:REGION:INSTANCE
DATABASE_URL=postgresql://mindweave:password@/mindweave_prod?host=/cloudsql/my-project:us-central1:mindweave-db

# Auth.js (NextAuth v5)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your_production_auth_secret_here
AUTH_URL=https://your-service-name.run.app

# Google OAuth (Production credentials)
AUTH_GOOGLE_ID=your_google_client_id
AUTH_GOOGLE_SECRET=your_google_client_secret

# Claude API (Production key)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Embedding Service (Production key)
OPENAI_API_KEY=your_openai_api_key_here

# App Configuration
NEXT_PUBLIC_APP_URL=https://your-service-name.run.app
NODE_ENV=production

# Note: In Cloud Run, these are injected from Secret Manager
# See cloudbuild.yaml and cloud-run-service.yaml for configuration
